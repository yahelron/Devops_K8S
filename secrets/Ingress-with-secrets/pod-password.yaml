apiVersion: v1
kind: Pod
metadata:
  name: password
  labels:
    app: password
spec:
  containers:
    - name: password
      image: nginxdemos/hello:plain-text
      ports:
        - containerPort: 80
      volumeMounts:
      - name: secret
        mountPath: "/usr/share/nginx/html"
        readOnly: true
  volumes:
    - name: secret
      secret:
        secretName: password
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-secret-svc-password
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: password
---
apiVersion: v1
kind: Secret
metadata:
  name: password
type: Opaque
data:
  password: cm9vdF91c2VyCg==